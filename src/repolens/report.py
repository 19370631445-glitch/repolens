"""PROJECT_MAP.md generation placeholders."""

from pathlib import Path
from typing import Any


def generate_project_map(summary: dict[str, Any], output_path: Path) -> Path:
    """Generate PROJECT_MAP.md in a future milestone."""
    del summary, output_path
    raise NotImplementedError("Report generation is not implemented yet.")

